#include <bits/stdc++.h>

using namespace std;

const int MAXN = 10010;

int main() {
    int T;
    scanf(" %d", &T);

    for (int t=0; t<T;t++) {
        char tmp[MAXN];
        scanf(" %s", tmp);
        bitset<MAXN> binario(tmp, tmp+strlen(tmp));

        

    }

}