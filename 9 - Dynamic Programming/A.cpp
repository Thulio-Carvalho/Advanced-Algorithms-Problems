#include <bits/stdc++.h>
#define debug(arr, len) cerr << #arr << " = "; for (int i=0;i<len;i++)cout << arr[i] << " \n"[i == n-1]
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    
    return 0;
}
