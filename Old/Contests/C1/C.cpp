#include <bits/stdc++.h>

using namespace std;

int maxVal, n, arr;

int main(){

  scanf("%d", &n);
  for(int i = 0; i < n; i++){
    scanf("%d", &arr[i]);
  }
  int sumArr[n];
  for(int i = )
  return 0;
}
